## Installieren

1. Dependencies installieren
   ```shell
   npm install
   ```
2. Umgebungsvariablen nach der `.env.example` setzen

3. Anwendung starten
   ```shell
   npm run
   # oder
   npm run dev
   ```
